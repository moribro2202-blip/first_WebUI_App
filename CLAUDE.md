# CLAUDE.md

このファイルは、Claude Code（claude.ai/code）がこのリポジトリで作業する際のガイドラインです。

## プロジェクト概要

Next.js (App Router) Web アプリケーションプロジェクト

## 重要ルール

- **既存UIの変更は事前承認が必須**
- **技術スタックのバージョン変更禁止**
- **要件に記載のない機能追加は禁止**
- **アイコンは Lucide（lucide-react）のみ使用**

詳細は各ドキュメントを参照。

## 実装ルール

1. 実装前に `docs/requirements/functional.md` の該当機能を確認
2. チェックボックス（- [ ]）が付いた項目のみ実装対象
3. 実装完了後、該当チェックボックスを `- [x]` に更新

## ドキュメント

### 場面別リファレンス

| 場面                   | 参照先                              |
| ---------------------- | ----------------------------------- |
| 開発環境を構築する     | `docs/SETUP.md`                   |
| タスクを分割する       | `docs/tasks/000_overview.md`      |
| 機能を実装する         | `docs/requirements/functional.md` |
| UIを作成する           | `docs/uiux.md`                    |
| ルーティングを追加する | `docs/router.md`                  |
| Firebaseを使う         | `docs/firebase.md`                |
| ビルド・デプロイする   | `docs/BUILD_RELEASE.md`           |
| エラーで困った         | `docs/TROUBLESHOOTING.md`         |

### ドキュメント一覧

| ドキュメント                | 内容                     |
| --------------------------- | ------------------------ |
| `docs/SETUP.md`           | 開発環境セットアップ     |
| `docs/BUILD_RELEASE.md`   | ビルド・デプロイ手順     |
| `docs/TROUBLESHOOTING.md` | トラブルシューティング   |
| `docs/requirements/`      | 要件定義書               |
| `docs/tasks/`             | タスク管理・ToDo         |
| `docs/techstack.md`       | 技術スタック・バージョン |
| `docs/router.md`          | ルーティング規約         |
| `docs/uiux.md`            | UI/UX設計ルール          |
| `docs/firebase.md`        | Firebase実装ガイド       |
| `docs/coding-rules.md`    | コーディング規約         |

## 環境変数

```
NEXT_PUBLIC_FIREBASE_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN
NEXT_PUBLIC_FIREBASE_PROJECT_ID
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID
```

## 知見蓄積のサイクル

- 他の場面でも発生し得る問題や効率化のノウハウを得た場合は、**必ず回答の最後に知見をまとめ、ルール更新を提案する**
- 新規にルールを作成した場合は、下の「ルールファイルの全体像」に追記する
- 提案されたルール改善は、ユーザー承認後に反映する

### 知見まとめの形式

```
---
📝 **知見メモ**
- [発見した問題/ノウハウ]

💡 **ルール改善提案**
- [具体的な改善案]
- 対象ファイル: [CLAUDE.md / docs/xxx.md / .claude/skills/xxx]
---
```

## ルールファイルの全体像

| ファイル                            | 種類     | 説明                             |
| ----------------------------------- | -------- | -------------------------------- |
| `CLAUDE.md`                       | メイン   | プロジェクト全体のガイドライン   |
| `docs/coding-rules.md`            | コード   | コーディング規約                 |
| `docs/uiux.md`                    | デザイン | UI/UX設計ルール                  |
| `docs/TROUBLESHOOTING.md`         | 運用     | エラー対処法                     |
| `.claude/skills/dev-flow/`          | スキル   | 開発フロー統合（実装→コミット→PR） |
| `.claude/skills/feature-start/`     | スキル   | 新機能開始（ブランチ→タスク分割） |
| `.claude/skills/git-commit/`        | スキル   | コミットメッセージ生成           |
| `.claude/skills/hotfix/`            | スキル   | 緊急修正（hotfixブランチ→修正→即PR） |
| `.claude/skills/release/`           | スキル   | リリース統合（ビルド→デプロイ→変更履歴） |
| `.claude/skills/code-review/`       | スキル   | コードレビュー（PR差分→分析→コメント） |
| `.claude/skills/stripe-setup/`      | スキル   | Stripe 決済設定                  |
| `.claude/skills/debugging-guide/`   | スキル   | デバッグ・トラブルシューティング |
| `.claude/skills/uiux-test/`         | スキル   | UI/UXテスト作成ガイド            |
| `.claude/skills/skill-name/`        | スキル   | スキル作成ガイド                 |

> ルールの説明を踏まえ、必要と判断したルールは積極的に事前確認すること。
>

## 競馬AIプロジェクトの決まり

### 絶対に守ること

1. **レース後にしか分からない値を、そのレースの予測に使わない**
   - 使ってはいけない列: 着順 / 上がり3F / 4角の位置 / 確定オッズ / 人気
   - 過去のレースのぶんは使ってよい（必ず `groupby().shift(1)` を通す）

2. **母集団を毎回数える**
   - データを読んだら、必ず「行数 / レース数 / 1レースあたりの頭数」を出す
   - 1レースあたりが 10 頭を下回ったら止まって報告する（データ欠けの疑い）

3. **「効いた」と言う前に、次の3つを必ず通す**
   - 人気をそろえて比べたか
   - 期間を2つに割って、両方で同じ向きに出たか
   - でたらめ（ラベルをシャッフルしたもの）と比べたか

4. **数字を報告するときは、必ず母数を添える**
   - 「複勝率28%」ではなく「複勝率28%（n=4,307）」と書く

5. **例外を握りつぶさない**
   - `except` で握り潰した結果を「データが無い」と報告しない
   - 想定と違う数字が出たら、まず自分の計器を疑う

### やらないこと

- 回収率が100%を超えると書かない（測っていないなら測っていないと書く）
- ユーザーが確認していないことを「確認しました」と書かない

### 開発フェーズ（この順序を守る。Phase 3の出口基準を通るまで買い目の話をしない）
```
Phase 1: データ基盤 — 時点整合(point-in-time)、リーク検知テスト常設
Phase 2: 市場ベースライン — β補正済み市場確率の対数尤度。以後の全モデルの基準線
Phase 3: 残差モデル — 条件付きロジットで「市場が織り込んでいない特徴量」を1つずつ審査
  出口基準: 市場ベースラインに対する対数尤度の改善が10年中8年以上でプラス
Phase 4: 確率→買い目 — EV十分位でGo/No-Go。85-90%ならPhase 3に戻る
Phase 5: 検証・凍結 — θ事前固定の入れ子WF、ブートストラップ95%CI、シャッフル
Phase 6: ペーパートレード→実弾 — 尤度で監視。再学習は年1回
```

### 原則
1. **目標は「当てる」ではなく「市場より当てる」** — 指標は市場ベースラインに対する対数尤度の改善量
2. **ハイパーパラメータも含めて全部ウォークフォワード** — α・λ・EV閾値・特徴量の採否まで
3. **分析には予算がある** — 事前に検証項目を固定し、有意になるまで定義をいじらない

### EV計算の統一定義
1. **EV = q_blend × 実オッズ**（閾値は1.0+マージン、券種別）
2. **q_blend = モデル確率と市場確率の対数線形ブレンド**（モデル単独で判定しない）
3. **ブレンド比α・閾値θはウォークフォワード内で推定**（テスト年のデータで決めたら過学習）
4. **券種確率はStern補正Harville（λはPhase 3で推定した真値用）**
5. **連系は券種プールの市場確率に再ブレンド**
6. **回収率100%超の報告基準**: θ事前固定の入れ子WF、ブートストラップ95%CI下限>100%、n≥3,000、10年中8年+

### 回収率計算の鉄則（厳守）
1. **回収率の払戻計算には必ずHJC確定オッズのみを使用する**
2. **HJC以外のオッズソースでの払戻計算は一切禁止**:
   - ts0（JRA-VAN確定オッズ）での払戻計算 → 禁止
   - confirmed_oddsでの払戻計算 → 禁止
   - SED確定オッズでの払戻計算 → 禁止
   - 1分前/3分前/5分前オッズでの払戻計算 → 禁止
   - OT/OZオッズでの払戻計算 → 禁止
3. **ハズレ時の払戻は0円** — HJCにデータがない的中もハズレ扱い（フォールバック禁止）
4. **オッズの推定・補間は禁止** — 実データ(HJC)にない値を推定で作らない
5. **HJCにデータがない券種・年は「データなし」として報告する**（他ソースで代用しない）
6. **EV判定（買うかどうかの判断）にはconfirmed_oddsや1分前オッズを使ってよい**
7. **実運用では締切直前オッズを参照** — 確定オッズ≒締切直前オッズ

### 禁止事項
- HJC以外のオッズでの回収率計算（ts0, confirmed_odds, SED, OT/OZ全て禁止）
- SED確定単勝からの他券種オッズ推定（不確実すぎる）
- HJCにない組合せのオッズをフォールバックで補完すること
- 「HJCがないからconfirmed_oddsで代用」は禁止（データをインポートして解決する）

## シミュレーション比較ルール

**シミュレーションを実施したら、必ず `docs/simulation-results.md` に結果を記録すること。**

### 記録ルール
1. バージョン番号（vN）を採番して追記する。**過去の結果は絶対に消さない**
2. 以下のフォーマットで記録する:

```markdown
## vN: [変更内容]（実施日）

### 変更点
- [何を変えたか]

### 条件
- リーク: あり/なし
- 補正: なし/1.05/時系列
- alpha: [値]
- 戦略: [三連複1点 等]

### ランキング

| # | モデル | R数 | 的中 | 的中率 | 投資 | 収支 | 回収率 |
|---|--------|-----|------|--------|------|------|--------|

### 1位の月別

| 月 | レース | 月回収率 | 累積回収率 | 累積損益 |
|----|--------|---------|-----------|---------|

### 前回との比較
- [改善点/悪化点]
```

3. 必ず前バージョンとの比較を書く
4. 未来データリークの有無を明記する
5. 現時点のベスト設定:
   - 戦略: 三連複◎○▲ 1点、固定10,000円
   - alpha: 0.15、特徴量: full（騎手+馬+距離+馬場+競馬場+トレンド）
   - 補正なし回収率: **110.4%**（v7）

### 関連ファイル
- `docs/simulation-results.md` — 全シミュレーション結果
- `docs/simulation_report_for_notebookLM.md` — レポート用資料
- `docs/ev-calculation.md` — EV計算理論
- `docs/odds-distortion.md` — オッズ歪み分析
- `scripts/simulate_*.py` — シミュレーションスクリプト
